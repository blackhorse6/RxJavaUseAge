package com.example.rxjavauseage;

public class RxJavaChange {
    private static final String TAG = "RxJavaChange";

//    public static void flatMap() {
//        Observable.just(UserDataUtil.INSTANCE.mock())
//                .flatMap((Function<User, ObservableSource<Course>>) user -> Observable.fromIterable(user.getList()))
//                .subscribe(course -> Log.d(TAG, course.getName()));
//    }
}
